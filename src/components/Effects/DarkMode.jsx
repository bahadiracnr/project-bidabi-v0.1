// import useDarkMode from '../../hooks/useDarkMode';
import '../../assets/styles/darkMode.css'; // Dark mode için gerekli CSS

const DarkModeToggle = () => {
  // const [isDarkMode, toggleDarkMode] = useDarkMode();

  return <></>;
};

export default DarkModeToggle;
